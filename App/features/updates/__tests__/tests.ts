import reducer from '../reducer'

const initialState = reducer(undefined, {} as any)

describe('updates', () => {
  it('should run a dummy test for now', () => {
    expect(true).toBeTruthy()
  })
})
