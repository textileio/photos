import FS from 'react-native-fs'

const config = {
  textileRepoPath: `${FS.DocumentDirectoryPath}/textile-go`,
  addingPhotosPath: `${FS.DocumentDirectoryPath}/textile-adding-photos`
}

export default config
